<Window x:Class="Malai.UI.Views.DtoWorkedHoursWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Malai.UI.Views"
        mc:Ignorable="d"
        Title="DtoWorkedHoursWindow" Height="450" Width="800">
    <Grid Margin="10">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <!-- Add more rows as needed -->
        </Grid.RowDefinitions>

        <Label Grid.Column="0" Grid.Row="0" Content="Employee Code"/>
        <TextBox Grid.Column="1" Grid.Row="0" Text="{Binding emp_code}"/>

        <Label Grid.Column="0" Grid.Row="1" Content="Client Code"/>
        <TextBox Grid.Column="1" Grid.Row="1" Text="{Binding clt_code}"/>

        <Label Grid.Column="0" Grid.Row="2" Content="Client Job Code"/>
        <TextBox Grid.Column="1" Grid.Row="2" Text="{Binding clt_job_code}"/>

        <Label Grid.Column="0" Grid.Row="3" Content="Start Time"/>
        <TextBox Grid.Column="1" Grid.Row="3" Text="{Binding start_time}"/>

        <Label Grid.Column="0" Grid.Row="4" Content="End Time"/>
        <TextBox Grid.Column="1" Grid.Row="4" Text="{Binding end_time}"/>

        <Label Grid.Column="0" Grid.Row="5" Content="Notes"/>
        <TextBox Grid.Column="1" Grid.Row="5" Text="{Binding notes}"/>

        <!-- Add more controls as needed -->

        <Button Grid.Column="1" Grid.Row="6" Content="Submit" Click="SubmitButton_Click"/>
    </Grid>
</Window>
